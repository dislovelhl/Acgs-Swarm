\* Expected result: TLC exit 12 and exactly
\* "Invariant CoverageGoalNotReached is violated." The counterexample must
\* include committed C3/C4 predecessor receipts followed by denial of revoked
\* attempt C2. This is a non-vacuity witness, not a safety failure. Exit 0 or
\* violation of any other invariant is a failed coverage check.
SPECIFICATION Spec

CONSTANTS
    Workflows = {w1, w2}
    W1 = w1
    W2 = w2
    Nodes = {root, child, leaf}
    Root = root
    Child = child
    Leaf = leaf
    Executors = {e1, e2}
    E1 = e1
    E2 = e2
    Attempts = {a1, a2}
    A1 = a1
    A2 = a2
    CommitIds = {c1, c2, c3, c4, c5, c6}
    C1 = c1
    C2 = c2
    C3 = c3
    C4 = c4
    C5 = c5
    C6 = c6
    ValidReceipts = {c1, c3, c4, c5, c6}
    NoCommit = none
    NoAttempt = noattempt
    NoExecutor = noexecutor

INVARIANT Invariant
INVARIANT CoverageGoalNotReached
